(function() {
	'use strict';

	angular
		.module('demo', ['angular-youtube'])
		.config(config)
		.run(run);

	/** @ngInject */
	function config($provide) {

	}

	/** @ngInject */
	function run($rootScope) {

	}

})();
