(function() {
	'use strict';

	angular
		.module('demo', ['angular-video-module'])
		.config(config)
		.run(run);

	/** @ngInject */
	function config($provide) {

	}

	/** @ngInject */
	function run($rootScope) {

	}

})();
